<nav class="navbar is-danger" role="navigation" aria-label="main navigation">
    <div class="container">
        <div class="navbar-brand">
            <%= link_to root_path, class: "navbar-item" do %>
                <%= image_tag "railsgirls-logo.png", size:"122x28" %>
            <% end %>

            <a role="button" class="navbar-burger burger" aria-label="menu" aria-expanded="false"
                data-target="navbarBasicExample">
                <span aria-hidden="true"></span>
                <span aria-hidden="true"></span>
                <span aria-hidden="true"></span>
            </a>
        </div>

        <div id="navbarBasicExample" class="navbar-menu">

            <div class="navbar-end">
                <div class="navbar-item">
                    <div class="buttons">
                        <% if user_signed_in? %>
                        <%= link_to "LOG OUT", destroy_user_session_path, method: :delete, class: "button has-background-white has-text-gray" %>
                        <% else %>
                        <%= link_to "Log In", new_user_session_path, class: "button is-light" %>
                        <%= link_to "Sign Up", new_user_registration_path, class: "button is-warning" %>
                        <% end %>
                    </div>
                </div>
            </div>
        </div>
    </div>
</nav>